# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Огляд проекту

Телеграм бот на Python для автоматизації запису моделей на безкоштовні косметологічні процедури. Бот використовує python-telegram-bot v20.7 та SQLite для зберігання даних.

**Мова інтерфейсу:** Українська (весь текст боту має бути українською)

## Останні важливі зміни

- ✅ **Уніфікація формату заявок**: одиночні та групові заявки тепер мають однаковий формат з номерами заявок
- ✅ **Динамічне оновлення статусів**: іконки статусів оновлюються в тексті повідомлень при зміні
- ✅ **Email-повідомлення**: автоматична відправка email адміністраторам при нових заявках
- ✅ **Підтримка кількох адмінів**: можна вказати кілька ADMIN_IDS через кому
- ✅ **Ротація логів**: автоматичне обмеження розміру файлів логів (10MB на файл, 5 бекапів)
- ✅ **Обмеження минулих заходів**: показується максимум 10 останніх минулих заходів

## Команди для роботи

### Запуск бота

```bash
python bot.py
# або
python3 bot.py
```

**ВАЖЛИВО:** Ніколи не запускай бота автоматично! Користувач робить це вручну.

### Встановлення залежностей

```bash
pip install -r requirements.txt
```

### Налаштування середовища

Скопіюйте `.env.example` в `.env` та налаштуйте змінні:

**Обов'язкові:**
- `BOT_TOKEN` - токен від @BotFather
- `ADMIN_IDS` або `ADMIN_ID` - Telegram user ID адміністратора(ів), розділені комами
- `EVENTS_GROUP_ID` - ID групи для публікації заходів (з мінусом!)
- `GROUP_ID` - ID групи для заявок (з мінусом!)
- `APPLICATIONS_CHANNEL_ID` - ID каналу для публікації заявок (з мінусом!)

**Опціональні:**
- `CHANNEL_LINK` - посилання на канал (https://t.me/...)
- `BOT_LOG_FILE` - файл логів (за замовчуванням: bot-actions.log)
- `EMAIL_ENABLED` - увімкнути email-повідомлення про заявки (true/false)
- `EMAIL_HOST`, `EMAIL_PORT`, `EMAIL_USER`, `EMAIL_PASSWORD`, `EMAIL_TO` - налаштування Gmail SMTP

## Архітектура

### Три основні компоненти:

1. **bot.py** (~4500 рядків) - Головний файл з логікою бота
   - ConversationHandler для створення заходів (множинне планування)
   - ConversationHandler для подачі заявок (модель)
   - ConversationHandler для блокування користувачів
   - ConversationHandler для керування типами процедур
   - Callback handlers для управління заявками в групі
   - Система автоочистки повідомлень адміністратора
   - Email-повідомлення про нові заявки (опціонально)
   - Підтримка кількох адміністраторів

2. **database.py** - Робота з SQLite
   - 5 таблиць: users, events, applications, application_photos, procedure_types
   - Методи CRUD для всіх сутностей
   - База створюється автоматично при першому запуску

3. **constants.py** - Константи та утиліти
   - PROCEDURE_TYPES - типи процедур (зараз 8 типів, включаючи "Видалення ПМ брів")
   - Стани для всіх ConversationHandler
   - generate_date_options() - генерує дати на 7 днів вперед
   - generate_time_slots() - генерує часові слоти 9:00-17:00 з інтервалом 10 хв
   - UKRAINE_TZ - часовий пояс Europe/Kyiv

### Основний потік даних:

```
Адмін створює заходи (множинне планування) → Публікація в канал (денне розкладу) →
→ Модель обирає захід(и) → Подає заявку з фото (якщо потрібно) →
→ Заявка в групу з хештегами → Адмін приймає/відхиляє →
→ Вибір основного кандидата → Автоматична відправка інструкцій
```

### ConversationHandler стани:

**Створення заходів (7 станів - множинне планування):**
- CREATE_EVENT_DATE → CREATE_EVENT_TIME → CREATE_EVENT_PROCEDURE →
  → CREATE_EVENT_PHOTO_NEEDED → CREATE_EVENT_COMMENT →
  → CREATE_EVENT_REVIEW (огляд розкладу) → CREATE_EVENT_CONFIRM

**Подача заявки (5 станів):**
- APPLY_SELECT_EVENTS (вибір заходів) → APPLY_FULL_NAME → APPLY_PHONE →
  → APPLY_PHOTOS → APPLY_CONFIRM

**Додаткові ConversationHandler:**
- BLOCK_USER_ID - блокування користувачів
- ADD_PROCEDURE_TYPE_NAME - додавання типів процедур
- EDIT_PROCEDURE_TYPE_NAME - редагування типів процедур
- CLEAR_DB_PASSWORD - очистка бази даних

## Важливі деталі реалізації

### Множинне планування заходів

- Адмін може створити кілька заходів на один день за один раз
- Процес: обирається дата → додаються процедури з часом → публікується денне розкладу
- Кнопки: "➕ Додати ще процедуру", "🗑 Видалити останню", "📋 Переглянути розклад"
- Публікація: всі заходи одного дня публікуються в каналі одним повідомленням
- Огляд розкладу показує всі заплановані заходи на обрану дату

### Множинний вибір заходів моделлю

- Модель може подати одну заявку на кілька заходів одночасно
- Після переходу з каналу показується список доступних заходів з чекбоксами
- Кнопка "✅ Вибрати всі" - вибирає всі доступні заходи
- Модель заповнює дані лише один раз для всіх обраних заходів
- В базі створюється окрема заявка (application) для кожного обраного заходу

### Фото від кандидатів

- **Адміністратор НЕ додає фото** при створенні заходу
- Адміністратор лише вибирає: "Чи потрібні фото від кандидатів?"
- Якщо `needs_photo=True`, кандидат **обов'язково** повинен додати мінімум 1 фото (максимум 3)
- Перевірка в `apply_photos_done()`: якщо фото обов'язкове, але не додано → помилка
- Кнопка "✅ Готово" завершує додавання фото
- Константа `MAX_APPLICATION_PHOTOS = 3` в bot.py

### Система автоочистки повідомлень адміністратора

- TTL для адмінських повідомлень: `ADMIN_MESSAGE_TTL = 15` секунд
- Використовується `context.user_data['admin_messages']` для відстеження
- Функції: `register_admin_dialog()`, `clear_admin_dialog()`, `delete_admin_message()`
- При переході між екранами старі повідомлення видаляються автоматично
- Очистка НЕ застосовується до GROUP_ID (повідомлення в групі залишаються)

### Денне зведення в групі

- Функція `build_day_summary_text()` створює зведення для конкретної дати
- Показує всі заходи дня з кількістю заявок за статусами
- Автоматичне оновлення при зміні статусу заявок через `update_day_summary()`
- Формат: "📅 День {дата}\n\n🕐 {час} - {процедура}\n  статистика заявок"
- Посилання на кожен захід для швидкого доступу

### Статуси заявок та автоматичне просування

- **pending** (⏳ Очікує) - початковий статус
- **approved** (✅ Резерв) - схвалено адміністратором
- **primary** (🌟 Основний) - обраний основний кандидат (тільки один на захід)
- **rejected** (❌ Відхилено) - відхилено адміністратором
- **cancelled** (🚫 Скасовано) - скасовано користувачем

Автоматичне просування:
- Якщо primary відміняє заявку → автоматично обирається наступний approved
- Функція `promote_next_candidate()` вибирає найстарішу approved заявку
- Новому primary автоматично надсилаються інструкції

### Групування заявок в групі

- Якщо користувач подав заявки на кілька заходів → одне групове повідомлення
- Функція `build_group_application_text()` формує список всіх заявок кандидата
- Кнопки для керування кожною заявкою окремо: схвалити/відхилити/обрати основним
- Повідомлення оновлюється при зміні статусу будь-якої заявки через `refresh_group_application_message()`

### Єдиний формат заявок

Одиночні та групові заявки мають однаковий формат:
```
Нова заявка від {ім'я}
Телефон: {телефон}
ID користувача: {user_id}

Обрана процедура: / Обрані процедури:
1. №{app_id} {дата} {час} — {процедура} {статус_іконка}
```

- Номер заявки (№ID) дозволяє ідентифікувати кожну заявку окремо
- Статус оновлюється автоматично при зміні (⏳→✅→🌟)
- Функції `refresh_single_application_message()` та `refresh_group_application_message()` оновлюють текст і клавіатуру

### Хештеги в групі

Заявки публікуються з двома хештегами для фільтрації:

- `#захід_{event_id}` - всі заявки на конкретний захід
- `#кандидат_{user_id}` - всі заявки від конкретного користувача

### Deep linking

Публікація заходів в каналі містить кнопку "📝 Подати заявку":

```
https://t.me/{bot_username}?start=apply
```

Обробляється в `start()` через `context.args`. Аргумент `apply` запускає діалог подачі заявки.

### Callback data patterns

**Створення заходів:**
- `date_{date}` - вибір дати
- `time_{time}` - вибір часу
- `proc_{index}` - вибір процедури
- `photo_yes/photo_no` - чи потрібні фото
- `skip_comment` - пропустити коментар
- `add_event` - додати ще одну процедуру
- `remove_last` - видалити останню процедуру
- `add_more` - додати ще процедуру після огляду
- `review_schedule` - переглянути розклад
- `publish_schedule` - опублікувати розклад
- `same_date` - створити ще захід на ту ж дату

**Управління заявками:**
- `approve_{application_id}` - прийняти заявку
- `reject_{application_id}` - відхилити заявку
- `primary_{application_id}` - встановити основним
- `view_apps_{event_id}` - переглянути заявки на захід
- `cancel_app_{application_id}` - скасувати заявку (користувач)
- `confirm_cancel_primary_{app_id}` - підтвердити скасування primary
- `cancel_primary_back_{app_id}` - повернутися назад

**Вибір заходів для заявки:**
- `toggle_event_{event_id}` - вибрати/скасувати захід
- `select_all_events` - вибрати всі заходи
- `reset_events` - скинути вибір
- `continue_events` - продовжити з вибраними заходами

**Адмінка:**
- `cancel_event_confirm_{event_id}` - підтвердити скасування заходу
- `admin_menu`, `user_menu` - повернутися до меню
- `close_msg` - закрити повідомлення
- `close_admin_dialog` - закрити діалог адміністратора

### Збереження даних користувачів

- Дані (ПІБ, телефон) зберігаються в таблиці `users` після першої заявки
- При наступних заявках пропонується вибір: "Використати збережені дані" або "Ввести нові"
- Функція `validate_ukrainian_phone()` перевіряє формат українського номера
- Номер має починатися з +380 і містити 12 цифр

## Типові помилки та виправлення

### F-string з апострофами

❌ НЕПРАВИЛЬНО:

```python
f"✅ Фото {'обов\'язкове' if x else 'ні'}"
```

✅ ПРАВИЛЬНО:

```python
text = "обов'язкове" if x else "ні"
f"✅ Фото {text}"
```

### ID каналу/групи

Завжди з мінусом: `-1001234567890`

### Права бота

- В каналі: право "Публікувати повідомлення"
- В групі: базові права адміністратора

## База даних

SQLite з 5 таблицями:

### users
- `user_id` INTEGER PRIMARY KEY
- `full_name` TEXT
- `phone` TEXT
- `is_blocked` INTEGER DEFAULT 0
- `created_at` TIMESTAMP

### events
- `id` INTEGER PRIMARY KEY AUTOINCREMENT
- `date` TEXT NOT NULL (формат: YYYY-MM-DD)
- `time` TEXT NOT NULL (формат: HH:MM)
- `procedure_type` TEXT NOT NULL
- `needs_photo` INTEGER DEFAULT 0
- `comment` TEXT (опціонально)
- `status` TEXT DEFAULT 'draft' (draft/published)
- `message_id` INTEGER (ID повідомлення в каналі)
- `created_at` TIMESTAMP

### applications
- `id` INTEGER PRIMARY KEY AUTOINCREMENT
- `event_id` INTEGER NOT NULL (FK → events.id)
- `user_id` INTEGER NOT NULL (FK → users.user_id)
- `full_name` TEXT NOT NULL
- `phone` TEXT NOT NULL
- `consent` INTEGER DEFAULT 0
- `status` TEXT DEFAULT 'pending' (pending/approved/primary/rejected/cancelled)
- `is_primary` INTEGER DEFAULT 0
- `group_message_id` INTEGER (ID повідомлення в групі)
- `created_at` TIMESTAMP

### application_photos
- `id` INTEGER PRIMARY KEY AUTOINCREMENT
- `application_id` INTEGER NOT NULL (FK → applications.id)
- `file_id` TEXT NOT NULL (Telegram file_id)

### procedure_types
- `id` INTEGER PRIMARY KEY AUTOINCREMENT
- `name` TEXT NOT NULL UNIQUE
- `is_active` INTEGER DEFAULT 1
- `created_at` TIMESTAMP

**Важливо:**
- Таблиці `event_photos` немає! Фото додають лише кандидати через `application_photos`.
- Типи процедур зберігаються в БД, а не в константах (з версії з динамічним керуванням)

## Керування типами процедур

### Динамічне керування (через бота)

В адмін-меню є розділ "⚙️ Типи процедур":
- Перегляд всіх типів з кількістю активних заходів
- Додавання нових типів
- Редагування назви існуючих
- Активація/деактивація типів
- Видалення типів (якщо немає пов'язаних заходів)

### Константа PROCEDURE_TYPES (constants.py)

Залишається для зворотної сумісності та початкової ініціалізації. При першому запуску типи з цього списку додаються в БД.

## Ключові функції для розуміння

### Створення заходів
- `create_event_start()` - початок діалогу
- `build_schedule_overview()` - формування огляду розкладу
- `publish_day_schedule_to_channel()` - публікація денного розкладу в канал

### Подача заявок
- `show_multi_event_selection()` - вибір заходів для заявки
- `apply_photos_done()` - завершення додавання фото
- `submit_application()` - фінальна підтвердження та збереження
- `publish_group_application_to_channel()` - публікація в групу

### Управління заявками
- `approve_application()` - схвалення заявки
- `reject_application()` - відхилення заявки
- `set_primary_application()` - встановлення primary
- `promote_candidate_to_primary()` - просування до primary з інструкціями
- `promote_next_candidate()` - автоматичне просування при скасуванні
- `refresh_group_application_message()` - оновлення групових заявок
- `refresh_single_application_message()` - оновлення одиночних заявок (текст + клавіатура)
- `send_email_notification()` - відправка email-повідомлень адміністраторам

### Форматування та утиліти
- `format_date()` - форматування дати для відображення
- `get_weekday_accusative()` - отримання дня тижня в знахідному відмінку
- `build_message_link()` - створення посилання на повідомлення
- `trim_text()` - обрізання тексту до певної довжини

## Обробка помилок

### Error handler
Функція `error_handler()` логує всі помилки та намагається відповісти користувачу повідомленням про помилку.

### Типові Telegram помилки
- `Forbidden` - користувач заблокував бота → встановлює `is_blocked=1`
- `BadRequest` - некоректний запит → логування та повідомлення користувачу
- `ChatMigrated` - група мігрувала → оновлення chat_id
- `TimedOut`, `NetworkError` - мережеві помилки → retry через bot API

## Логування

- Основний файл логів: `BOT_LOG_FILE` з .env (за замовчуванням `bot-actions.log`)
- Рівень логування: DEBUG для детальної інформації
- Логується в консоль + файл (якщо вказано)
- Формат: `%(asctime)s - %(levelname)s - %(name)s - %(message)s`
- **Ротація логів:** RotatingFileHandler з максимальним розміром 10MB, 5 резервних копій
- Загальний максимальний розмір логів: ~60MB

## Email-повідомлення

При увімкненій опції (`EMAIL_ENABLED=true`) бот автоматично відправляє email при новій заявці:

**Налаштування Gmail SMTP:**
1. Увімкнути двоетапну верифікацію в Google Account
2. Створити App Password: Google Account → Безпека → Паролі додатків
3. Додати в .env:
   ```env
   EMAIL_ENABLED=true
   EMAIL_HOST=smtp.gmail.com
   EMAIL_PORT=587
   EMAIL_USER=your_email@gmail.com
   EMAIL_PASSWORD=xxxx xxxx xxxx xxxx  # 16-значний App Password
   EMAIL_TO=admin1@example.com,admin2@example.com
   ```

**Функціонал:**
- Автоматична відправка при успішній подачі заявки
- Підтримка кількох адрес (через кому)
- HTML форматування для кращого відображення
- Логування успіху/помилок відправки

## Важливі обмеження та правила

### Заборонено автоматично
- **Запуск/перезапуск бота** - користувач робить це вручну
- **Додавання фото адміністратором** - фото додають лише кандидати
- **Більше 3 фото на заявку** - обмеження `MAX_APPLICATION_PHOTOS = 3`
- **Більше одного primary на захід** - лише один основний кандидат

### Обов'язкова валідація
- Номер телефону має бути українським (+380...)
- Дата заходу не може бути в минулому (для today - час також перевіряється)
- Фото обов'язкове якщо `needs_photo=True` для заходу
- ID каналу/групи мають починатися з мінуса

### Налаштування користувача (context.user_data)
- `admin_messages` - список повідомлень для автоочистки
- `selected_event_ids` - вибрані заходи при поданні заявки
- `application_photos` - тимчасово збережені file_id фото
- `schedule` - тимчасовий розклад при множинному плануванні

## Особливості взаємодії з Telegram API

### Inline клавіатури
- Максимум 8 кнопок в ряду для оптимального відображення
- Callback data обмежена 64 байтами
- Використовується `chunk_list()` для розбиття на ряди

### Повідомлення
- `parse_mode=ParseMode.HTML` для форматування
- `disable_web_page_preview=True` для посилань
- Екранування HTML через `html.escape()`

### Обробка помилок відправки
- При `Forbidden` - позначення користувача як заблокованого
- При помилці в групі - логування без краху бота
- Retry логіка для мережевих помилок

## Тестування змін

### Перед запуском нових змін
1. Перевір що всі ConversationHandler мають fallbacks
2. Переконайся що callback_data унікальні
3. Перевір що всі тексти українською
4. Переконайся що фото НЕ додаються адміністратором

### При зміні структури БД
1. Створи міграцію (додавання колонок через ALTER TABLE)
2. Перевір що існуючі дані не пошкоджені
3. Тестуй на копії БД, а не на production

### При додаванні нових callback handlers
1. Додай pattern в main()
2. Перевір що відповідь надсилається через `answer_callback_query()`
3. Переконайся що повідомлення оновлюється або видаляється

## Інтеграція з каналом та групою

### Публікація в канал
- Денне розкладу публікується одним повідомленням
- Містить список всіх заходів дня з часом та процедурою
- Кнопка "📝 Подати заявку" веде в бота з deep link
- `message_id` зберігається для можливого оновлення

### Публікація в групу
- Групові повідомлення для кандидатів з кількома заявками
- Оновлення при зміні статусу будь-якої заявки
- Хештеги для фільтрації (#захід_X, #кандидат_Y)
- Денне зведення оновлюється автоматично

### Управління в групі
- Кнопки approve/reject/primary для кожної заявки
- Перегляд фото кандидата (якщо додані)
- Посилання на профіль кандидата
- Статистика заявок за статусами
